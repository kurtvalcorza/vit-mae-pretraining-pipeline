# Release status

Current status: **Candidate** — the `E2E` tutorial notebook `tutorials/vit_mae_pretraining_colab.ipynb` has not yet executed top-to-bottom in a clean supported runtime. The pipeline package with its reconstruction, probe and adaptation contracts, the offline unit suite, the model-backed regressions, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), the static validator (`tools/validate_release_assets.py`), the generator parity checks and the CI workflow exist and are green, and the default tutorial path has been replayed through the package API on the build workstation's CPU (`docs/release-verification.md`), but that is a pre-flight, not promotion evidence. The status becomes **Release-grade** only when a clean run of the exact committed notebook blob is recorded there; any later change to the carried modules or to the notebook yields a new blob and returns the registry to Candidate.
